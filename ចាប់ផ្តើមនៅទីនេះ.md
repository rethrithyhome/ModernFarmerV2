# ចាប់ផ្តើមនៅទីនេះ

កញ្ចប់នេះរួចរាល់ស្រាប់។ មិនចាំបាច់កែឯកសារណាមួយទេ។

---

## ១. Supabase — បង្កើត database

1. **supabase.com** → **New project** → ឈ្មោះ `modern-farmer` → Region **Singapore**
2. **Database Password**: ចុច Generate → **ចម្លងកត់ទុក**
3. រង់ចាំ ២ នាទី

**បញ្ចូលតារាង** — ចុច **SQL Editor** → **New query** → ចម្លងខ្លឹមសារឯកសារ → **Run**។ ធ្វើ ៤ ដងតាមលំដាប់៖

```
1. db/schema.sql
2. db/migration_002_sales_finance.sql
3. db/migration_003_offline.sql
4. db/migration_004_users.sql
5. db/seed.sql
```

**យកលេខតភ្ជាប់** — ចុចប៊ូតុង **Connect** នៅខាងលើទំព័រ → ជ្រើស **Transaction pooler** → ចម្លងខ្សែអក្សរ → ជំនួស `[YOUR-PASSWORD]` ដោយពាក្យសម្ងាត់ពីជំហាន ២។

---

## ២. GitHub — ផ្ទុកកូដ

បង្កើត repository ថ្មី **ទទេទាំងស្រុង** (កុំធីក "Add a README file")។

រួចនៅក្នុងថតនេះ រត់៖

```bash
git init
git add .
git commit -m "Modern Farmer V2"
git branch -M main
git remote add origin https://github.com/<username>/<repo>.git
git push -u origin main
```

> បើប្រើ GitHub Desktop: **Add** → **Add existing repository** → ជ្រើសថតនេះ → **Publish repository** (ដោះធីក "Keep this code private" បើចង់ឱ្យសាធារណៈ)។

---

## ៣. Vercel — ដាក់ដំណើរការ

1. **vercel.com** → **Add New** → **Project** → **Import** repository
2. **Framework Preset**: ទុក **Other**
3. **Environment Variables** — បញ្ចូល ៦ ធាតុ៖

| Name | Value |
|---|---|
| `DATABASE_URL` | ខ្សែអក្សរពីជំហាន ១ |
| `DB_SSL` | `true` |
| `JWT_SECRET` | អក្សរចៃដន្យវែង ៤០+ តួ |
| `SEED_ADMIN_EMAIL` | `admin@modernfarmer.kh` |
| `SEED_ADMIN_PASSWORD` | `ChangeMe123!` |
| `CRON_SECRET` | អក្សរចៃដន្យមួយទៀត |

4. **Deploy**

---

## ៤. សាកល្បង

បើកតំណដែល Vercel ផ្តល់ឱ្យ → ចូលដោយ៖

- `admin@modernfarmer.kh`
- `ChangeMe123!`

**ប្តូរពាក្យសម្ងាត់ភ្លាមៗ។**

---

## សាកលើកុំព្យូទ័រខ្លួនឯងជាមុន (ស្រេចចិត្ត)

```bash
npm install
cd web && npm install && npm run build && cd ..
```

បង្កើតឯកសារ `.env` (ចម្លងពី `.env.example`) ដាក់ `DATABASE_URL` និង `JWT_SECRET` រួចរត់៖

```bash
npm start
```

បើក `http://localhost:3000`

---

### ឯកសារឡូហ្គោ

ឡូហ្គោនៅ `web/public/` — `logo.png` (ពេញ) · `logo-mark.png` (រូបប៉ុណ្ណោះ) · កំណែពណ៌ស សម្រាប់ផ្ទៃងងឹត · និងរូបតំណាងកម្មវិធី។ ជំនួសឯកសារទាំងនេះ បើប្តូរឡូហ្គោពេលក្រោយ។

---

📘 លម្អិតពេញលេញ · ការដោះស្រាយបញ្ហា · ការដំឡើងលើ VPS → **DEPLOY.md**
📗 ព័ត៌មានបច្ចេកទេស · API · អ្វីដែលបានតេស្ត → **README.md**
