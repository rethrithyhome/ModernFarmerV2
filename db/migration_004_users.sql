-- ============================================================
-- កសិករទំនើប — Migration 004
-- ផ្ទាំងគ្រប់គ្រងអ្នកប្រើ
--
-- បញ្ហាដែលដោះស្រាយ: មុននេះ ការបិទគណនី ឬប្តូរពាក្យសម្ងាត់
-- មិនមានប្រសិទ្ធភាពភ្លាមទេ — token ចាស់នៅប្រើបានរហូតដល់ផុតកំណត់ (១២ ម៉ោង)។
-- បើបុគ្គលិកឈប់ការងារ គាត់នៅចូលប្រព័ន្ធបានពេញមួយថ្ងៃ។
--
-- ដំណោះស្រាយ: កត់ពេលវេលាប្តូរពាក្យសម្ងាត់។ Token ណាដែលចេញមុនពេលនោះ
-- ត្រូវបដិសេធភ្លាម។ រួមនឹងការត្រួតពិនិត្យ is_active រាល់សំណើ។
-- ============================================================

BEGIN;

ALTER TABLE users
  ADD COLUMN IF NOT EXISTS password_changed_at TIMESTAMPTZ NOT NULL DEFAULT now();

COMMIT;
